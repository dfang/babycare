%h1 Listing checkins

%table
  %thead
    %tr
      %th Name
      %th Mobile phone
      %th Birthdate
      %th Gender
      %th Email
      %th Job
      %th Employer
      %th Nationality
      %th Province
      %th City
      %th Area
      %th Address
      %th Source
      %th Remark
      %th
      %th
      %th

  %tbody
    - @checkins.each do |checkin|
      %tr
        %td= checkin.name
        %td= checkin.mobile_phone
        %td= checkin.birthdate
        %td= checkin.gender
        %td= checkin.email
        %td= checkin.job
        %td= checkin.employer
        %td= checkin.nationality
        %td= checkin.province_id
        %td= checkin.city_id
        %td= checkin.area_id
        %td= checkin.address
        %td= checkin.source
        %td= checkin.remark
        %td= link_to 'Show', checkin
        %td= link_to 'Edit', edit_checkin_path(checkin)
        %td= link_to 'Destroy', checkin, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Checkin', new_checkin_path
