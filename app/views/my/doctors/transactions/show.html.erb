<%- if resource.withdraw? %>
  提现
<% else %>
  收入
<% end %>

<%= resource.amount %>元
<% if resource.reservation.present? %>
  , 来自预约<%= link_to resource.reservation.try(:reservation_title), my_doctors_reservation_path(resource.reservation) %>,
  <%= link_to "查看该用户的资料", profile_my_doctors_patient_path(resource.reservation.user_a), data: { turbolinks: "false" } %>
<% end %>
